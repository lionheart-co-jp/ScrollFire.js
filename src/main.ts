/**
 * ScrollFire
 *
 * # Sample
 * var fire = new ScrollFire();
 * fire.addTrigger(element, entryFunction, leaveFunction);
 * fire.start();
 *
 * @version 0.2.0
 */

import "./scss/styles.scss";

import { ScrollFire } from "./lib/ScrollFire";

export default ScrollFire;
import "./lib/InitAnimation";
